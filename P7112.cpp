#include<bits/stdc++.h>
#define int long long
using namespace std;
const int N=605;
int n,p;
int a[N][N];
signed main(){
    ios::sync_with_stdio(0);cin.tie(0);
    cin>>n>>p;
    for(int i=1;i<=n;i++)
        for(int i=1;i<=n;i++)
            cin>>a[i][j];
    int ans=0;
    return 0;
}