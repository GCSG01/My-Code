T1,用时超过预期，要注意分配时间。

$n^4$ 不难想到，手模样例可以发现对于固定左上角，右上角的一个偏移量固定，则其余三个角的变化轨迹为与x,y轴平行的线以及一条对角线。
注意到题解提供了 bitset 卡常，所以用 bitset 直接维护就做完了。~~我还以为有什么ds能维护~~

T2，由于T1用时太长了，导致没调完。

注意到 $a_i$ 之间是因数关系。通过数据可知，一样的 $a_i$ 直接合并，又注意到 $a_i\le 1\time 10^18$，所以 $m$ 只有64左右，直接枚举每一个 $a_i$ 对答案的贡献即可。注意到是自然溢出，同时又有除法，所以要先处再乘。

**总结**：要改变时间分配，严格遵守，不要死磕。